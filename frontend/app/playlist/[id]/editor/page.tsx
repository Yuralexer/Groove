"use client"; // Обязательно первая строка

import React from "react"; // <--- ВАЖНО: Добавь этот импорт, иначе будет краш

export default function EditPage() {
    return (
        <div style={{ padding: 50, color: 'white' }}>
            <h1>Страница редактирования работает!</h1>
        </div>
    );
}